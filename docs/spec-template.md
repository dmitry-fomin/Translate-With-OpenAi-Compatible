# Spec: <название фичи>

> Заполняй сверху вниз. Не переходи к коду, пока **все** секции не закрыты и не утверждены.

## 1. Why (зачем)
- Какую проблему/боль решаем?
- Чей сценарий выигрывает (мой как переводчика? читателя?)?

## 2. Acceptance Criteria
- Как **проверим**, что задача сделана? Список Given/When/Then.
- Команда(ы), которые должны вернуть успешный результат.

## 3. Scope
**In scope:**
- ...

**Out of scope (явно НЕ делаем):**
- ...

## 4. Edge Cases
- Что с очень длинными главами (> лимита токенов)?
- Что при ошибке сети / 5xx от LLM?
- Что при невалидном EPUB / отсутствующем `.opf`?
- Что при повторном запуске (кеш саммари)?
- Что с не-OpenAI-совместимыми ответами провайдера?

## 5. Technical Decisions
- Какие интерфейсы из `src/Domain/` затрагиваются?
- Какие реализации в `src/Infrastructure/` создаём/меняем?
- Новые зависимости в `composer.json`? (если да — обоснование)

## 6. Risks (Pre-mortem)
> Заполняется через `/premortem`. Минимум 5 сценариев провала.

## 7. Plan (после approve спеки)
- [ ] Шаг 1: падающий тест `tests/...`
- [ ] Шаг 2: реализация ...
- [ ] Шаг 3: интеграция в `bin/translate` / `.env.example`
- [ ] Шаг 4: гейты (`composer check`)
- [ ] Шаг 5: запись решения через `bd remember`

## 8. Approval
- [ ] Спека утверждена (пользователь)
- [ ] План утверждён (пользователь)
- [ ] Гейты зелёные
- [ ] Reviewer-этап пройден
