---
description: Pre-mortem текущего плана — стресс-тест перед кодом
---

Прошло 6 месяцев. План из текущей задачи провалился в продакшене перевода книги.

Выступи в роли сурового критика и ответь:
1. Что именно сломалось (минимум 5 сценариев).
2. Какие гипотезы в плане оказались ошибочны.
3. Какие edge cases не были предусмотрены — особенно вокруг:
   - длинных глав и лимита токенов (`TextSplitter`),
   - не-OpenAI-совместимых ответов LLM,
   - порчи EPUB при `EpubHandler::save`,
   - конкурентного запуска (общий `.summary_cache.json`),
   - утечек ключей API в логи.
4. Список правок плана, которые нужно сделать **до** написания кода.

Ничего не кодируй. Только разбор.
